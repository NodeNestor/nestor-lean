---
description: Turn nestor-lean back on after /nestor-lean:off — active from the next tool call
---

Run this command and show the user its output verbatim. Do not add commentary
beyond one short line if something looks wrong.

```
python "${CLAUDE_PLUGIN_ROOT}/hooks/switch.py" on
```
