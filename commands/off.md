---
description: Turn nestor-lean off for this machine — tool output passes through untouched, no restart needed
---

Run this command and show the user its output verbatim. Do not add commentary
beyond one short line if something looks wrong.

```
python "${CLAUDE_PLUGIN_ROOT}/hooks/switch.py" off
```
