---
description: Show cumulative token savings from nestor-lean (read dedup + grep compression)
---

Run this command and show the user its output verbatim, then add one short
sentence of interpretation:

```
python "${CLAUDE_PLUGIN_ROOT}/hooks/stats.py"
```
