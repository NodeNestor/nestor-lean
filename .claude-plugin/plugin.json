{
  "name": "nestor-lean",
  "version": "0.5.0",
  "description": "Input-side token compression for Claude Code: read dedup, differential reads, codemap skill, command-output compression (real rtk + built-in routes), and MCP tool-output compression. Context-aware, multi-agent safe, tee-backed, fails open.",
  "author": {
    "name": "NodeNestor"
  },
  "hooks": "./hooks/hooks.json"
}